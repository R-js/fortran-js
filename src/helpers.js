'use strict'

const _ = undefined
const cc = '\u0020'
const isString = c => typeof c === 'string'
const isObject = c => typeof c === 'object' && c !== null

module.exports = {
	_,
	cc,
	isString,
	isObject
}